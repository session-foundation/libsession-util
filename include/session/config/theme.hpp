#pragma once

namespace session::config {

enum class theme {
    defaulted = 0,
    classic_dark = 1,
    classic_light = 2,
    ocean_dark = 3,
    ocean_light = 4,
};

enum class theme_primary_color {
    defaulted = 0,
    green = 1,
    blue = 2,
    yellow = 3,
    pink = 4,
    purple = 5,
    orange = 6,
    red = 7,
};

}  // namespace session::config
